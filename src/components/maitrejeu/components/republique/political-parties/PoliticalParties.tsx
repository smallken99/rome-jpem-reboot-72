
import React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export const PoliticalParties: React.FC = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Partis politiques</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Interface de gestion des partis politiques à implémenter</p>
      </CardContent>
    </Card>
  );
};
